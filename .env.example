# This app does not require any build-time secrets: Firebase config lives in
# src/firebase-applet-config.json (public client config, safe to expose) and
# all privileged operations run through Cloud Functions (see /functions),
# which get their credentials automatically from the Firebase project.
